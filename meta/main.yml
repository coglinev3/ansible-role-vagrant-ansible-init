---
# file: roles/vagrant-ansible-init/meta/main.yml

galaxy_info:
  author: Cogline.v3
  description: This role performs the initial Ansible configuration in a Vagrant environment.
  license: BSD
  min_ansible_version: 2.4
  #github_branch:
  platforms:
    - name: EL
      versions:
      - 7
  galaxy_tags:
    - vagrant
    - ansible
    - ssh
    - hosts

dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
