---
# file: roles/vagrant-ansible-init/meta/main.yml

galaxy_info:
  author: Cogline.v3
  description: This role performs the initial Ansible configuration in a multi node Vagrant environment.
  license: BSD
  min_ansible_version: 2.7
  #github_branch:
  platforms:
    - name: EL
      versions:
      - 6
      - 7
    - name: Ubuntu
      versions:
      - xenial
      - bionic
      - cosmic
    - name: Debian
      versions:
      - jessie
      - stretch
  galaxy_tags:
    - vagrant
    - ansible
    - ssh
    - keys
    - hosts

dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
