---
# file: roles/vagrant-ansible-init/meta/main.yml

galaxy_info:
  author: Cogline.v3
  role_name: vagrant_ansible_init
  namespace: coglinev3
  description: >-
    This role performs the initial Ansible configuration in a multi node
    Vagrant environment.
  license: BSD
  min_ansible_version: "2.14"
  platforms:
    - name: Alpine
      versions:
        - "any"
    - name: EL
      versions:
        - 7
        - 8
        - 9
    - name: Debian
      versions:
        - stretch
        - buster
        - bullseye
    - name: Ubuntu
      versions:
        - bionic
        - focal
        - jammy
  galaxy_tags:
    - vagrant
    - ansible
    - ssh
    - keys
    - hosts

dependencies: []
# List your role dependencies here, one per line.
# Be sure to remove the '[]' above if you add dependencies
# to this list.
